(() => {
  const API='https://api.github.com/repos/fernand21/installerlab-web/releases?per_page=100';
  const RELEASES='https://github.com/fernand21/installerlab-web/releases';
  const FALLBACK={tag_name:'v3.0.0',name:'InstallerLab v3.0.0 — Specialized Installers, Smart Build Targets & Release Tooling',html_url:'https://github.com/fernand21/installerlab-web/releases/tag/v3.0.0',published_at:'2026-09-10T16:19:04Z',assets:[
    {name:'InstallerLab-Setup.exe',size:512908288,download_count:0,digest:'sha256:f199cd08c652ebe690e6cae9ac7642cd3de922e7adf314ae60158a49f28d4e39',browser_download_url:'https://github.com/fernand21/installerlab-web/releases/download/v3.0.0/InstallerLab-Setup.exe'},
    {name:'InstallerLab-Setup.msi',size:405930734,download_count:0,digest:'sha256:707995d5976c28bb64aa49579163b496642f24f91569444976d1481df09ea8a7',browser_download_url:'https://github.com/fernand21/installerlab-web/releases/download/v3.0.0/InstallerLab-Setup.msi'},
    {name:'InstallerLab_Bundle.exe',size:408917715,download_count:0,digest:'sha256:0822753130aa2c6079db67a77f56f449209b12ed5853ddea8815b753bbed4fc8',browser_download_url:'https://github.com/fernand21/installerlab-web/releases/download/v3.0.0/InstallerLab_Bundle.exe'},
    {name:'InstallerLab_Portable.exe',size:413324800,download_count:0,digest:'sha256:1c4377deeed4991e5ccff222db56be5aad73f0086af4a069d5ceb0ac72c4a676',browser_download_url:'https://github.com/fernand21/installerlab-web/releases/download/v3.0.0/InstallerLab_Portable.exe'}]};
  let promise=null,queued=false;
  const lang=()=>((localStorage.getItem('il-lang')||'es').toLowerCase()==='en'?'en':'es');
  const fmt=n=>new Intl.NumberFormat(lang()).format(Number(n||0));
  const size=n=>n?`${(n/1024/1024).toFixed(2)} MB`:'—';
  const date=v=>v?new Intl.DateTimeFormat(lang(),{year:'numeric',month:'long',day:'numeric'}).format(new Date(v)):'';
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const isPortable=a=>/portable/i.test(a?.name||'')&&/\.exe$/i.test(a?.name||'');
  const isBundle=a=>/bundle/i.test(a?.name||'')&&/\.exe$/i.test(a?.name||'');
  const isMsi=a=>/\.msi$/i.test(a?.name||'');
  const isSetup=a=>/\.exe$/i.test(a?.name||'')&&!isPortable(a)&&!isBundle(a);
  const digest=a=>a?.digest?a.digest.replace(/^sha256:/i,''):'';

  function getData(){if(promise)return promise;promise=fetch(API,{headers:{Accept:'application/vnd.github+json'}}).then(r=>r.ok?r.json():Promise.reject()).then(list=>{const releases=(Array.isArray(list)?list:[]).filter(r=>!r.draft&&!r.prerelease);if(!releases.length)throw 0;return{releases,live:true};}).catch(()=>({releases:[FALLBACK],live:false}));return promise;}
  function stats(releases){const out={total:0,setup:0,bundle:0,portable:0,msi:0};releases.forEach(r=>(r.assets||[]).forEach(a=>{const n=Number(a.download_count||0);out.total+=n;if(isPortable(a))out.portable+=n;else if(isBundle(a))out.bundle+=n;else if(isMsi(a))out.msi+=n;else if(isSetup(a))out.setup+=n;}));return out;}
  function card(asset,type,title,copy,count,badge=''){if(!asset)return'';const l=lang();return `<article class="release-card${badge==='recommended'?' recommended':''}">${badge?`<span class="tag">${badge==='recommended'?(l==='es'?'RECOMENDADO':'RECOMMENDED'):(l==='es'?'V3':'V3')}</span>`:''}<div class="file-type">${type}</div><h3>${title}</h3><p>${copy}</p><div class="release-meta"><span>${l==='es'?'Archivo':'File'} <strong>${esc(asset.name)}</strong></span><span>${l==='es'?'Tamaño':'Size'} <strong>${size(asset.size)}</strong></span><span>${l==='es'?'Descargas':'Downloads'} <strong>↓ ${fmt(count)}</strong></span></div><a class="release-download" href="${esc(asset.browser_download_url)}">${l==='es'?'Descargar directamente':'Direct download'} ↓</a>${digest(asset)?`<div class="release-hash" title="SHA-256">SHA-256 · ${digest(asset)}</div>`:''}</article>`;}

  function renderDownload(releases,live){
    if(document.body.dataset.page!=='download')return;
    const main=document.querySelector('main.content'); if(!main)return;
    const current=releases.find(r=>r.tag_name==='v3.0.0')||releases[0]||FALLBACK,st=stats(releases),assets=current.assets||[],l=lang();
    const setup=assets.find(isSetup),bundle=assets.find(isBundle),portable=assets.find(isPortable),msi=assets.find(isMsi);
    const hero=document.querySelector('.page-hero .shell'); if(hero)hero.innerHTML=l==='es'?`<span class="eyebrow">Descargas</span><h1>InstallerLab v3.0.0</h1><p>Setup EXE, Bundle, Portable EXE y MSI oficiales de la versión 3, con tamaños, SHA-256 y contadores sincronizados con GitHub Releases.</p>`:`<span class="eyebrow">Downloads</span><h1>InstallerLab v3.0.0</h1><p>Official v3 Setup EXE, Bundle, Portable EXE and MSI downloads with sizes, SHA-256 and counters synchronized with GitHub Releases.</p>`;
    main.innerHTML=`<div class="release-live"><section class="release-hero"><div class="release-panel"><span class="release-kicker"><i></i>${l==='es'?'Versión pública actual':'Current public release'}</span><h2>${esc(current.name||FALLBACK.name)}</h2><p>${l==='es'?'v3 añade proyectos especializados Office Add-in y QGIS Plugin, Smart Build Targets, CLI, SBOM, firma digital, Services y mejoras de localización, manteniendo Setup EXE, Portable, MSI y Bundle para aplicaciones normales.':'v3 adds specialized Office Add-in and QGIS Plugin projects, Smart Build Targets, CLI, SBOM, digital signing, Services and localization improvements while keeping Setup EXE, Portable, MSI and Bundle for normal applications.'}</p><div class="release-links"><a class="button" href="${esc(current.html_url||FALLBACK.html_url)}" target="_blank" rel="noopener">${l==='es'?'Ver v3 en GitHub ↗':'View v3 on GitHub ↗'}</a><a class="button" href="${RELEASES}" target="_blank" rel="noopener">${l==='es'?'Todas las versiones ↗':'All releases ↗'}</a></div></div><aside class="release-summary"><div class="release-total">↓ ${fmt(st.total)}</div><div class="release-total-label">${l==='es'?'descargas totales de archivos oficiales':'total official asset downloads'}</div><div class="release-version-line"><span>${l==='es'?'Publicada':'Published'} <strong>${date(current.published_at)}</strong></span><span>${live?'GitHub API · live':(l==='es'?'Datos v3 de respaldo':'v3 fallback data')}</span></div></aside></section><section class="release-grid">
      ${card(setup,'EXE','Setup EXE',l==='es'?'Recomendado para una instalación normal. Incluye el flujo usado para preparar WiX cuando vas a crear MSI o Bundle.':'Recommended for a normal installation. Includes the workflow used to prepare WiX when you plan to build MSI or Bundle.',st.setup,'recommended')}
      ${card(bundle,'BUNDLE','Bundle EXE',l==='es'?'Bootstrapper WiX Burn de InstallerLab v3. También demuestra el mismo backend Bundle utilizado por proyectos Application, Office Add-in y QGIS Plugin.':'InstallerLab v3 WiX Burn bootstrapper. It demonstrates the same Bundle architecture used by Application, Office Add-in and QGIS Plugin projects.',st.bundle,'new')}
      ${card(portable,'EXE','Portable EXE',l==='es'?'Ejecuta InstallerLab sin instalación tradicional. La edición Portable de InstallerLab no es lo mismo que la función PRO que crea portables de otras aplicaciones.':'Runs InstallerLab without a traditional installation. The InstallerLab Portable edition is different from the PRO feature that creates portable packages for other applications.',st.portable)}
      ${card(msi,'MSI',l==='es'?'Paquete MSI':'MSI package',l==='es'?'Windows Installer real generado por el backend WiX. Útil para msiexec y despliegues administrados.':'Real Windows Installer package generated by the WiX backend. Useful for msiexec and managed deployment.',st.msi)}
      </section><div class="release-note">${l==='es'?'<strong>WiX para crear MSI/Bundle:</strong> InstallerLab v3 está preparado para WiX 7.x. Bundle usa <code>WixToolset.BootstrapperApplications.wixext</code> y, cuando la detección de prerrequisitos lo requiere, <code>WixToolset.Util.wixext</code>.':'<strong>WiX for MSI/Bundle creation:</strong> InstallerLab v3 targets WiX 7.x. Bundle uses <code>WixToolset.BootstrapperApplications.wixext</code> and, when prerequisite detection requires it, <code>WixToolset.Util.wixext</code>.'}</div></div>`;
  }

  const H={
    'v3.0.0':{
      es:['Office Add-ins VBA para Excel, Word y PowerPoint como proyectos especializados.','QGIS Python Plugins desde carpeta/ZIP con perfil, PluginId y lifecycle especializado.','Smart Build Targets: la UI habilita solo formatos válidos según ProjectType.','CLI headless para analyze, build y SBOM.','SBOM en CycloneDX y SPDX.','Firma Authenticode mediante SignTool y verificación posterior.','Windows Services dentro del modelo FSS para Application.','Resolución automática genérica de idioma y Bundle reutilizando el MSI especializado.'],
      en:['Specialized Excel, Word and PowerPoint VBA Office Add-in projects.','QGIS Python Plugins from folder/ZIP with profile, PluginId and specialized lifecycle.','Smart Build Targets: UI enables only valid outputs for the active ProjectType.','Headless CLI for analyze, build and SBOM.','CycloneDX and SPDX SBOM generation.','Authenticode signing through SignTool with post-sign verification.','Windows Services in the FSS model for Application projects.','Generic automatic language resolution and Bundle reuse of specialized MSI logic.']
    },
    'v2.0.0':{es:['WiX Burn Bundle como salida de primera clase.','FSS Analyzer e importación ISS → FSS.','Mejoras del backend MSI y pipeline ZERO-TRASH.'],en:['First-class WiX Burn Bundle output.','FSS Analyzer and ISS → FSS import.','MSI backend improvements and ZERO-TRASH pipeline.']}
  };
  function generic(r){const lines=String(r.body||'').split(/\r?\n/).map(x=>x.trim()).filter(x=>/^[-*]\s+/.test(x)).map(x=>x.replace(/^[-*]\s+/,'').replace(/\*\*/g,'')).slice(0,8);return lines.length?lines:[lang()==='es'?'Consulta las notas completas en GitHub.':'See full notes on GitHub.'];}
  function renderChangelog(releases){if(document.body.dataset.page!=='changelog')return;const main=document.querySelector('main.content');if(!main)return;const l=lang();const hero=document.querySelector('.page-hero .shell');if(hero)hero.innerHTML=l==='es'?'<span class="eyebrow">Versiones</span><h1>InstallerLab v3 y versiones anteriores.</h1><p>Historial público con artefactos oficiales y cambios principales.</p>':'<span class="eyebrow">Releases</span><h1>InstallerLab v3 and previous releases.</h1><p>Public history with official assets and major changes.</p>';main.innerHTML=`<div class="release-live"><div class="release-history">${releases.map((r,i)=>{const h=H[r.tag_name]?.[l]||generic(r),downloads=(r.assets||[]).reduce((n,a)=>n+Number(a.download_count||0),0);return `<article class="release-history-card"><div class="release-history-head"><div><h2>${esc(r.name||r.tag_name)}</h2><p>${date(r.published_at)} · ↓ ${fmt(downloads)} ${l==='es'?'descargas':'downloads'}</p></div>${i===0?`<span class="release-history-badge">${l==='es'?'ACTUAL':'CURRENT'}</span>`:''}</div><ul class="release-highlights">${h.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="release-assets-mini">${(r.assets||[]).map(a=>`<a href="${esc(a.browser_download_url)}">${esc(a.name)} · ${size(a.size)} · ↓ ${fmt(a.download_count)}</a>`).join('')}</div><div class="release-links"><a class="button" href="${esc(r.html_url)}" target="_blank" rel="noopener">${l==='es'?'Notas completas en GitHub ↗':'Full notes on GitHub ↗'}</a></div></article>`;}).join('')}</div></div>`;}
  function home(releases){if(document.body.dataset.page!=='home')return;const current=releases.find(r=>r.tag_name==='v3.0.0')||releases[0]||FALLBACK,setup=(current.assets||[]).find(isSetup),st=stats(releases),actions=document.querySelector('.hero-actions');if(!actions)return;const primary=actions.querySelector('a.button.primary');if(primary&&setup){primary.href=setup.browser_download_url;primary.textContent=lang()==='es'?'Descargar v3.0.0 →':'Download v3.0.0 →';}let counter=actions.querySelector('.home-live-counter');if(!counter){counter=document.createElement('span');counter.className='home-live-counter';actions.appendChild(counter);}counter.textContent=`↓ ${fmt(st.total)} ${lang()==='es'?'descargas':'downloads'}`;}
  function apply(){getData().then(({releases,live})=>{renderDownload(releases,live);renderChangelog(releases);home(releases);});}
  function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;apply();});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply);else apply();
  new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});
})();
